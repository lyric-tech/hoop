// Single source of truth for the product name shown in the UI. Deliberately
// NOT applied to functional identifiers — cookie names, window events, the
// `hoop.dev/` connection-tag prefix and the release download URLs all keep
// their old names because a peer, a browser or the gateway reads them.
export const PRODUCT_NAME = 'Lyric IAM'
