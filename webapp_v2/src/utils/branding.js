// Single source of truth for the product name shown in the UI. Deliberately
// NOT applied to functional identifiers (the `hoop` CLI, cookie names,
// window events) or to upstream docs URLs.
export const PRODUCT_NAME = 'Lyric IAM'
